#include <stdio.h>

int main() {
int a, sqr;
printf("Enter Any Number\n");
scanf("%d", &a);

printf("Entered number is %d\n", a);

sqr = a * a;
    printf("Square of entered number is: %d", sqr);
    return 0;
}
